#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import io.intino.tara.language.model.rules.variable.VariableRule;

public class ${NAME} implements VariableRule<${TYPE}>{

	@Override
	public boolean accept(${TYPE} value){
		return false; //TODO
	}
}
